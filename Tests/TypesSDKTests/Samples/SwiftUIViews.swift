import SwiftUI

struct HelloView: View {
    var body: some View {
        Text("Hello, World!")
    }
}

struct QualifiedView: SwiftUI.View {
    var body: some View {
        Text("Qualified!")
    }
}
