{
  "setupCommands": [
    {
      "command": "TUIST_VERSION=$(grep '^tuist' .tool-versions | awk '{print $2}') && echo '[tools]' > .tool-versions && echo \"tuist $TUIST_VERSION\" >> .tool-versions"
    },
    {
      "command": "mise install",
      "workingDirectory": "DodoPizza"
    },
    {
      "command": "tuist install",
      "workingDirectory": "DodoPizza"
    },
    {
      "command": "tuist generate --no-open",
      "workingDirectory": "DodoPizza"
    }
  ],
  "buildSettingsParameters": ["SWIFT_VERSION", "SWIFT_STRICT_CONCURRENCY", "OTHER_SWIFT_FLAGS"],
  "workspaceName": "DodoPizza",
  "configuration": "Debug"
}